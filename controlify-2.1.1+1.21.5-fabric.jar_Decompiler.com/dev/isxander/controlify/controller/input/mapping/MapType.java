package dev.isxander.controlify.controller.input.mapping;

public enum MapType {
   BUTTON,
   AXIS,
   HAT,
   NOTHING;

   // $FF: synthetic method
   private static MapType[] $values() {
      return new MapType[]{BUTTON, AXIS, HAT, NOTHING};
   }
}
