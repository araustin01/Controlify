package dev.isxander.controlify.api.ingameguide;

public enum ActionLocation {
   LEFT,
   RIGHT;

   // $FF: synthetic method
   private static ActionLocation[] $values() {
      return new ActionLocation[]{LEFT, RIGHT};
   }
}
